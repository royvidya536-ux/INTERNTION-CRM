# Postgres credentials (also used by odoo.conf / docker-compose)
DB_USER=odoo
DB_PASSWORD=odoo

# Odoo web ports
ODOO_PORT=8069
ODOO_LONGPOLLING_PORT=8072

# Optional pgAdmin (docker compose --profile tools up)
PGADMIN_EMAIL=admin@interntion.co.uk
PGADMIN_PASSWORD=admin
PGADMIN_PORT=5050
